# 인터엑스 온보딩 포털 기획안

## 0. 확정된 방향 (사용자 결정 사항)
- 메인 콘텐츠 포맷: **A안 - 카드 스와이프 + 미니퀴즈**
- 게이미피케이션 축: **스트릭 + 포인트 + 배지**
- AI 우선 기능: **온보딩 AI 챗봇**
- 배포 인프라: **Railway (백엔드 + MySQL + 프론트 모두)**

## 1. 핵심 기능 목록
1. 온보딩 대시보드(홈) — 주차별 미션 카드, 스트릭/포인트/배지 요약, 진행률바
2. 핵심가치 학습 모듈 — 12개 가치 각각: 인트로 카드 → 상황 카드 2~3장 → 미니퀴즈 → 포인트/배지 획득
3. 주차별 미션 시스템 — 인사팀이 미션 등록, 신입사원이 인증 제출, 완료 시 스트릭 증가
4. 게이미피케이션 엔진 — 스트릭 카운터, 포인트 적립, 가치별 배지 12종 + 특별 배지
5. 온보딩 AI 챗봇 — 핵심가치/사내 정보 Q&A, 학습 콘텐츠 안내, 반복 질문 패턴 감지 시 인사팀 알림(옵션)
6. 피드백 채널 — 신입사원 ↔ 인사팀 양방향 피드백(미션별 코멘트, 주간 회고)
7. 관리자(인사팀) 대시보드 — 신입사원별 진행현황, 미션 관리(CRUD), 피드백 열람
8. 인증/계정 — 로그인(JWT), 심사용 테스트 계정 별도 제공

## 2. 정보구조 (IA)
- 랜딩/로그인
- 홈 대시보드(신입사원용): 이번주 미션 / 나의 진행률(스트릭·포인트·배지) / 핵심가치 학습 허브(12개 카드 그리드)
- 핵심가치 상세(카드스와이프+퀴즈 플로우)
- 미션 상세/제출
- AI 챗봇(플로팅 버튼)
- 마이페이지(배지함, 히스토리)
- 관리자 대시보드(인사팀용, 별도 role): 신입사원 목록/진행현황, 미션 관리, 피드백함

## 3. 화면 흐름
로그인 → 웰컴 튜토리얼(1회성) → 홈 대시보드 → [핵심가치 카드 클릭] → 카드스와이프 학습 → 퀴즈 → 결과(포인트+배지 연출) → 홈 복귀
→ [이번주 미션 클릭] → 미션 상세 → 인증 제출 → 완료 애니메이션(스트릭 증가)
AI 챗봇은 전 화면에서 플로팅 아이콘으로 상시 접근

## 4. DB 스키마 초안 (MySQL)
- `users` (id, name, email, password, role[EMPLOYEE/ADMIN], join_date, department)
- `core_values` (id, name, icon, description, sort_order)
- `value_cards` (id, core_value_id, card_type[INTRO/SITUATION/QUIZ], content, sort_order)
- `quiz_options` (id, card_id, text, is_correct)
- `user_value_progress` (id, user_id, core_value_id, status, completed_at, quiz_score)
- `missions` (id, week_number, title, description, created_by)
- `user_missions` (id, user_id, mission_id, status, submission_text, submitted_at, feedback)
- `badges` (id, name, icon, condition_type, condition_value)
- `user_badges` (id, user_id, badge_id, earned_at)
- `streaks` (id, user_id, current_streak, longest_streak, last_active_date)
- `points_log` (id, user_id, source_type, source_id, points, created_at)
- `feedbacks` (id, user_id, mission_id(nullable), content, created_at, reply)
- `chatbot_logs` (id, user_id, question, answer, created_at) — 옵션

## 5. 기술 스택 확정
- Backend: Spring Boot(Java) + Spring Data JPA + Spring Security(JWT)
- DB: MySQL (Railway 호스팅)
- Frontend: React(또는 Next.js) + Framer Motion(카드 스와이프/마이크로 인터랙션)
- AI 챗봇: LLM API 연동(핵심가치/사내정보 기반 RAG 방식)
- 배포: Railway 단일 프로젝트(백엔드+MySQL+프론트)

## 6. 게이미피케이션 규칙
- 스트릭: 하루 1회 이상 활동(학습카드 완료 or 미션 제출) 시 +1, 미접속 시 리셋
- 포인트: 가치 학습 완료 +10 / 퀴즈 정답 +5 / 미션 제출 +20 / 미션 승인 보너스 +10
- 배지: 가치별 12종(가치 완료 시) + 스트릭 배지(7일/14일) + 올클리어 배지(전체 완료)
